# Builds the module for sambamba
!include sambamba.yaml
!include rcic-module.yaml
---
- package: sambamba Module
  category: BIOTOOLS
  module:
    setenv: ""
    prepend_path:
      - PATH {{root}}/bin
