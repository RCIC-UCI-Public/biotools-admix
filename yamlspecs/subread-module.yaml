# Builds the module for subread.
!include subread.yaml
!include rcic-module.yaml
---
- package: subread Module
  category: BIOTOOLS
  module:
    prepend_path:
      - PATH {{root}}/bin
    setenv: ""
