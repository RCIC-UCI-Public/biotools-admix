!include common.yaml
---
- package: Genome Analysis Toolkit
  name: gatk 
  release: 2
  version: "{{versions.gatk}}"
  vendor_source: https://github.com/broadinstitute/gatk/archive/{{version}}.{{extension}}
  description: >
    GATK {{version}}. Genome Analysis Toolkit 
  build:
    modules: 
      - java/{{versions.java}}
  install:
     makeinstall: >
       mkdir -p $(ROOT){{root}};
       $(INSTALL) gatk $(ROOT){{root}};
       $(INSTALL) -m 0644 gatk*.* GATK* README.md $(ROOT){{root}};
       $(TAR) cf - gatkdoc scripts | (cd $(ROOT){{root}}; tar xf -)
  requires:
    - jre = {{versions.java}} 
  rpmJarRepack: 0
