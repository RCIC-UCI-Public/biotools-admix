# Builds the module for bwa.
!include bwa.yaml
!include rcic-module.yaml
---
- package: BWA Burrows-Wheeler Aligner Module
  category: BIOTOOLS
  module:
    prereq:
      - *ModuleCompiler_prereq
    requires:
      - *ModuleCompiler_requires
