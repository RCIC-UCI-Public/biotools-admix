!include racon.yaml
!include rcic-module.yaml
---
- package: racon module
  category: BIOTOOLS
  release: 2
  module:
    setenv:
    prepend_path:
      - PATH {{root}}/bin
    prereq:
      - "{{build.base_modules}}"
