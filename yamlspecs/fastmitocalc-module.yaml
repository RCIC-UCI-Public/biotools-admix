# Builds the module for fastmitocalc
!include fastmitocalc.yaml
!include rcic-module.yaml
---
- package: fastmitocalc Module
  category: BIOTOOLS
  module:
    setenv: 
      - MITO_HOME {{root}}
    prepend_path:
      - PATH {{root}}/bin
