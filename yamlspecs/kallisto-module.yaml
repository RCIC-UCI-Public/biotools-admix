# Builds the module for kallisto.
!include kallisto.yaml
!include rcic-module.yaml
---
- package: kallisto Module
  category: BIOTOOLS
  module:
    prereq: 
      - *ModuleCompiler_prereq
      - hdf5/{{versions.hdf5}}/{{compiler}}.{{compiler_version}}
      - java/{{versions.java}}
    requires:
      - *ModuleCompiler_requires
      - hdf5_{{versions.hdf5}}_{{compiler}}_{{compiler_version}}-module
      - java{{versions.javamajor}}-module
