# Builds the module for vg.
!include vg.yaml
!include rcic-module.yaml
---
- package: vg Module
  category: BIOTOOLS
  module:
    prereq:
      - *ModuleCompiler_prereq
    requires:
      - *ModuleCompiler_requires
